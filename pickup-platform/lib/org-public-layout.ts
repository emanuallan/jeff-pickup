/** Public org pages: mobile stays narrow; desktop gets a wider, more polished layout. */
export const ORG_PUBLIC_CONTENT_MAX = 'max-w-lg lg:max-w-5xl'

/** Desktop overrides for shell bottom padding (mobile calc padding is unchanged below lg). */
export const ORG_PUBLIC_DESKTOP_SHELL_PADDING = 'lg:pb-12'
